{
  "name": "book_system",
  "version": "1.0.0",
  "description": "Book-Library",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build-win": "electron-packager . Library --platform=win32 arch=all --asar --overwrite",
    "build-mac": "electron-packager . Library --platform=mas arch=all --asar --overwrite"
  },
  "author": "allen",
  "license": "ISC",
  "devDependencies": {
    "electron": "^13.1.8"
  },
  "dependencies": {
    "@electron/remote": "^1.2.1",
    "bytenode": "^1.3.3",
    "electron-packager": "^15.3.0"
  }
}
